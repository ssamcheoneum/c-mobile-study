# C언어 모바일 학습 웹앱 — 개발 명세서

> Claude Code 투입용. 리포 루트에 `SPEC.md`로 저장하고 **Stage 단위로 순서대로** 진행할 것.
> 각 Stage 완료 시 반드시 멈추고 완료 기준(DoD) 체크 결과를 보고한 뒤 다음 단계로 넘어간다.

---

## 0. 프로젝트 개요

### 0.1 목적
이동 중·자투리 시간에 **휴대폰으로** C언어를 능동적으로 학습한다. 읽기만 하는 자료가 아니라
"단원 선택 → 요약 / 개념 / 퀴즈 / 기타" 구조로 손가락이 계속 움직이는 학습 도구를 만든다.

### 0.2 사용자
- CS 전공 학부생 1인. C언어 초보~초중급.
- 목표 수준: 운영체제 수업(포인터·메모리) 대비 가능한 정도.
- 사용 환경: **모바일 세로 화면 전용 설계**(데스크톱은 중앙 정렬로 보이기만 하면 됨).
- 사용 상황: 지하철·대기시간 등 5~15분 단위. 한 손 조작. 네트워크 불안정 가능.

### 0.3 커리큘럼 (10단원 고정)
| # | 단원 |
|---|---|
| 1 | 기초 입출력문 |
| 2 | 연산자 |
| 3 | 제어문 |
| 4 | 반복문 |
| 5 | 매크로 |
| 6 | 배열과 포인터 |
| 7 | 함수 |
| 8 | 변수의 통용범위 |
| 9 | 구조체 |
| 10 | 파일 관리 |

### 0.4 비목표 (명시적으로 하지 않는 것)
- 브라우저 내 C 컴파일/실행(WASM 컴파일러 등). → **"출력 예측 → 정답 공개"** 방식으로 대체한다.
- 로그인, 서버, DB, 계정 동기화. → 전부 기기 로컬 저장.
- 프레임워크(React/Vue), 번들러(Vite/webpack), TypeScript, npm 의존성. → **전부 금지.**
- 데스크톱 전용 레이아웃, 다국어 지원.

---

## 1. 기술 형식 결정 (확정)

| 항목 | 결정 | 이유 |
|---|---|---|
| 형식 | 정적 HTML + 바닐라 CSS/JS (ES Modules) | 빌드 단계 없음 → 수정 즉시 반영, 유지보수 부담 최소 |
| 콘텐츠 | 단원별 `content/chXX.json` 분리 | 단일 HTML에 다 넣으면 파일이 수천 줄이 되어 Claude Code가 매번 전체를 재작성해야 함. 콘텐츠 추가가 코드 수정과 분리됨 |
| 배포 | GitHub Pages (`main` 브랜치 루트 또는 `/docs`) | 폰에서 URL만 열면 됨. 로컬 `file://` 방식은 iOS/안드로이드 모두 파일 접근이 번거롭고 `fetch()`가 CORS로 막힘 |
| 설치 | PWA (manifest + Service Worker) | 홈 화면 추가 → 앱처럼 실행, 오프라인 열람 가능 |
| 저장 | `localStorage` | 진도·오답 데이터는 수십 KB 수준. IndexedDB는 과잉 |
| 폰트 | 시스템 폰트 스택 + 코드용 고정폭 | 웹폰트 로딩 지연 없음 |

> **단일 HTML 파일 1개**로 만들지 않는 이유: 10단원 콘텐츠가 다 들어가면 초기 로딩과 편집이 모두 무거워진다. 대신 **앱 셸은 가볍게, 콘텐츠는 필요한 단원만 fetch**하는 구조를 택한다.

---

## 2. 폴더 구조 (Stage 0에서 이 형태로 생성)

```
c-study-mobile/
├─ index.html              # 앱 셸 (단일 페이지, 해시 라우팅)
├─ manifest.webmanifest
├─ sw.js                   # 서비스 워커 (오프라인 캐시)
├─ css/
│  └─ style.css            # 디자인 토큰 + 전체 스타일
├─ js/
│  ├─ app.js               # 부트스트랩, 라우터
│  ├─ router.js            # 해시 라우팅
│  ├─ store.js             # localStorage 래퍼 (진도/오답/설정)
│  ├─ loader.js            # 단원 JSON fetch + 캐시
│  ├─ render.js            # 블록 타입 → DOM 렌더러
│  ├─ quiz.js              # 퀴즈 엔진
│  └─ review.js            # 복습 큐 로직
├─ content/
│  ├─ index.json           # 단원 목록 메타
│  ├─ ch01.json ~ ch10.json
│  └─ glossary.json        # 용어사전
├─ icons/                  # PWA 아이콘 192/512
├─ SPEC.md                 # 이 문서
└─ README.md
```

---

## 3. 데이터 스키마 (⚠️ 이 스키마가 프로젝트의 계약. 변경 시 전 단계 재검토)

### 3.1 `content/index.json`
```json
{
  "version": "1.0",
  "chapters": [
    { "id": "ch01", "order": 1, "title": "기초 입출력문",
      "subtitle": "printf / scanf 와 서식 지정자",
      "estMin": 25, "status": "ready" }
  ]
}
```
`status`: `"ready"` | `"draft"` | `"empty"` — `empty`는 목록에서 잠금 표시.

### 3.2 `content/chXX.json`
```json
{
  "id": "ch06",
  "title": "배열과 포인터",
  "summary": {
    "oneLiner": "한 문장 요약",
    "bullets": ["핵심 3~6개", "..."],
    "keywords": [ { "term": "포인터", "desc": "주소를 담는 변수" } ]
  },
  "preview": "다음 단원(함수)에서는 이 포인터를 인자로 넘겨 값을 바꾸는 방법을 다룹니다.",
  "sections": [
    {
      "id": "6-1",
      "title": "배열의 메모리 배치",
      "body": [
        { "type": "p",    "text": "본문 문단. **굵게**, `코드` 인라인 마크업 지원." },
        { "type": "code", "lang": "c", "code": "int a[3] = {1,2,3};", "caption": "선언과 초기화" },
        { "type": "tip",  "tone": "info",  "text": "말풍선 팁 내용" },
        { "type": "tip",  "tone": "trap",  "text": "자주 하는 실수" },
        { "type": "list", "items": ["항목1", "항목2"] },
        { "type": "table","head": ["서식", "의미"], "rows": [["%d","정수"]] },
        { "type": "predict", "code": "printf(\"%d\", 5/2);",
          "answer": "2", "explain": "정수 나눗셈이므로 소수점 버림" }
      ]
    }
  ],
  "quiz": [
    { "id": "ch06-q1", "type": "mcq", "level": 2, "tags": ["포인터"],
      "stem": "다음 중 옳은 것은?", "code": null,
      "choices": ["a", "b", "c", "d"], "answer": 1,
      "explain": "정답 해설" },
    { "id": "ch06-q2", "type": "ox",     "stem": "...", "answer": true,  "explain": "..." },
    { "id": "ch06-q3", "type": "blank",  "stem": "정수 출력 서식은 %__ 이다.",
      "answer": ["d"], "explain": "..." },
    { "id": "ch06-q4", "type": "output", "stem": "출력 결과는?",
      "code": "...", "choices": ["1","2","3","컴파일 에러"], "answer": 3, "explain": "..." }
  ]
}
```

**블록 타입 6종만 지원**: `p` / `code` / `tip` / `list` / `table` / `predict`.
**퀴즈 타입 4종만 지원**: `mcq` / `ox` / `blank` / `output`.
그 외 타입은 렌더러에서 무시하고 콘솔 경고만 남긴다. (스키마 확장 = 별도 Stage)

`tip.tone`: `info`(참고) / `trap`(함정·자주 하는 실수) / `preview`(앞으로 배울 내용 예고).
`level`: 1(암기) / 2(이해) / 3(적용).

### 3.3 localStorage 키
| 키 | 값 |
|---|---|
| `cstudy:progress` | `{ "ch01": { "sectionsRead": ["1-1"], "quizBest": 0.8, "lastAt": 1699… } }` |
| `cstudy:wrong` | `[ { "qid":"ch06-q1", "chId":"ch06", "count":2, "lastAt":…, "cleared":false } ]` |
| `cstudy:queue` | `{ "qid": { "due": 1699…, "streak": 1 } }` |
| `cstudy:settings` | `{ "theme":"auto", "fontScale":1 }` |

모든 읽기/쓰기는 `store.js`를 통해서만. JSON 파싱 실패 시 기본값으로 복구하고 앱은 죽지 않는다.

---

## 4. 화면 명세

### 4.1 라우팅 (해시 기반)
```
#/                     홈 = 단원 목록
#/ch/ch06              단원 허브 (요약/개념/퀴즈/기타 탭)
#/ch/ch06/concept/6-1  개념 섹션
#/ch/ch06/quiz         퀴즈 실행
#/review               복습 큐
#/wrong                오답노트
#/glossary             용어사전
```
뒤로가기(브라우저/제스처)가 항상 자연스럽게 동작해야 한다. 퀴즈 진행 중 이탈 시 확인 없이 나가되 진행 상태는 버린다.

### 4.2 홈 — 단원 목록
- 상단: 전체 진도 요약 1줄(예: "10단원 중 3단원 완료 · 오답 12문항").
- 단원 카드 10개 세로 나열. 각 카드: 번호 · 제목 · 부제 · 진도 막대 · 예상 소요시간.
- `status:"empty"` 단원은 흐리게 + "준비 중" 표시, 탭 불가.
- 하단 고정 바: **복습 / 오답노트 / 용어사전** 진입.
- 복습 대기 문항이 있으면 홈 최상단에 "오늘 복습 N문항" 배너 노출(탭 시 `#/review`).

### 4.3 단원 허브 — 4탭
탭은 상단 고정 세그먼트. 탭 전환은 페이지 리로드 없이.

**① 요약**
한 문장 요약 → 핵심 불릿 → 키워드 칩 목록(탭하면 용어 설명 바텀시트). 60초 안에 훑을 수 있는 분량.

**② 개념**
섹션 목록 → 섹션 진입 시 블록 렌더링. 읽은 섹션은 체크 표시.
- `code` 블록: 가로 스크롤 허용(줄바꿈 금지), 우상단 복사 버튼.
- `tip` 블록: 말풍선 형태. `tone`별 색·아이콘 구분.
- `predict` 블록: 코드만 먼저 보이고 **"출력 예측하기"** 입력창 + 확인 버튼 → 정답/해설 공개. 정답 여부는 채점만 하고 저장하지 않는다.
- 섹션 하단: "다음 섹션" 버튼, 마지막 섹션이면 "퀴즈 풀기" 버튼.
- 단원 마지막에 `preview` 문구를 별도 박스로 노출("다음 단원 예고").

**③ 퀴즈**
- 진입 화면: 문항 수 선택(5 / 10 / 전체), 시작 버튼.
- 문항 1개씩 표시. 선택 즉시 채점하고 해설을 펼친다(제출 버튼 없음 → 모바일 탭 수 절감).
- 오답이면 자동으로 `cstudy:wrong`에 기록하고 복습 큐에 넣는다.
- 상단 진행 표시(현재/전체, 얇은 막대).
- 종료 화면: 점수, 틀린 문항 목록, "오답만 다시 풀기" 버튼.

**④ 기타**
해당 단원의 부가 자료. 다음 항목을 카드로:
- 자주 하는 실수 체크리스트(`tip.tone === "trap"` 자동 수집)
- 이 단원 관련 컴파일 에러 메시지 사전(에러 문구 → 원인 → 조치)
- 이 단원 키워드 목록
- 진도 초기화 버튼(확인 후 실행)

### 4.4 복습 큐 (`#/review`)
간격 반복의 최소 구현.
- 정답 시 다음 노출: `streak` 0→1일, 1→3일, 2→7일, 3→17일, 4 이상은 큐에서 제거(`cleared:true`).
- 오답 시 `streak = 0`, 다음 날 재등장.
- 큐가 비면 "지금 복습할 문항이 없습니다. 새 단원을 시작하세요." + 홈 이동 버튼.

### 4.5 오답노트 (`#/wrong`)
단원별 그룹. 각 항목: 문제 요약 · 틀린 횟수 · 마지막 시각. 탭하면 해설 전문. "이 단원 오답만 풀기" 버튼.

---

## 5. 디자인 방향

학습용 도구다. **장시간 읽어도 눈이 편한 저채도 배경 + 정보 구분용 강조색 2~3개**로 간다.
장식적 그라디언트, 카드마다 그림자 반복, 전면 애니메이션 금지.

### 5.1 토큰 (CSS 변수로 `:root`에 정의)
```
--bg:        #F3F4F1   /* 매우 옅은 세이지 그레이. 순백 대비 눈부심 완화 */
--surface:   #FFFFFF
--ink:       #1E2A24   /* 본문 */
--ink-soft:  #5A6660   /* 보조 텍스트 */
--line:      #DDE2DC
--accent:    #2E6F52   /* 진행·정답·주요 액션 (딥그린) */
--warn:      #A8571F   /* 함정·주의 팁 */
--wrong:     #9E3B3B   /* 오답 */
--code-bg:   #16211C   /* 코드 블록 배경(다크 고정) */
--code-fg:   #E6EDE8
```
다크 모드는 `prefers-color-scheme: dark`로 `--bg:#151A17`, `--ink:#E7EDE9` 등 대응. 야간 지하철 사용을 전제로 **반드시 구현.**

### 5.2 타이포
- 본문 17px / line-height 1.7, 한 줄 40자 내외.
- 코드 15px 고정폭, `-webkit-text-size-adjust:100%`.
- 설정에서 글자 크기 3단계(0.9 / 1.0 / 1.15 배율) 조절 가능.

### 5.3 모바일 조작 규칙
- 모든 탭 대상 최소 44×44px.
- 주요 액션 버튼은 화면 하단 1/3 영역(엄지 도달 범위).
- 가로 스크롤은 코드 블록·표 내부에서만 발생. 페이지 전체는 절대 가로 스크롤 없음.
- 스크롤 위치는 라우트별로 기억했다가 뒤로가기 시 복원.

---

## 6. 개발 단계 (Stage)

각 Stage는 **독립 커밋**. DoD를 만족하지 못하면 다음 Stage로 넘어가지 않는다.

### Stage 0 — 리포 골격 + 스키마 확정
1. 위 폴더 구조 생성(빈 파일 포함).
2. `content/index.json`에 10단원 메타 작성(`ch01`만 `ready`, 나머지 `empty`).
3. `content/ch01.json`을 **스키마 전 기능을 사용하는 샘플**로 작성: 블록 6종 전부, 퀴즈 4종 전부 최소 1개씩 포함. 내용은 기초 입출력문(printf/scanf/서식 지정자) 실제 내용으로 채운다.
4. `README.md`에 로컬 실행 방법(`python3 -m http.server 8000`) 기재.

**DoD**: 로컬 서버로 `index.html`이 열리고, `ch01.json`이 `fetch`로 파싱되어 콘솔에 출력된다.

---

### Stage 1 — 앱 셸 + 라우터 + 홈
1. `index.html`: 헤더 / `<main id="view">` / 하단 고정 바.
2. `router.js`: `hashchange` 기반. 알 수 없는 경로는 홈으로.
3. `store.js`: 4개 키에 대한 get/set/기본값 복구.
4. 홈 화면 렌더링: 단원 카드 10개, `empty` 잠금 처리, 진도 막대(현재는 0%).
5. `css/style.css`: §5 토큰과 기본 타이포·레이아웃 확립. 다크 모드 포함.

**DoD**: 폰 사이즈(390×844)에서 홈이 가로 스크롤 없이 표시되고, `ch01` 카드 탭 시 `#/ch/ch01`로 이동하며 빈 허브가 뜬다.

---

### Stage 2 — 단원 허브 + 요약 탭 + 콘텐츠 로더
1. `loader.js`: 단원 JSON fetch, 메모리 캐시, 실패 시 에러 화면(재시도 버튼).
2. 4탭 세그먼트 UI. 탭 상태를 해시에 반영.
3. 요약 탭 완성(한 줄 요약 / 불릿 / 키워드 칩 → 바텀시트).

**DoD**: 오프라인(개발자도구 Network offline)에서 fetch 실패 시 앱이 죽지 않고 에러 화면을 보여준다.

---

### Stage 3 — 개념 탭 + 블록 렌더러
1. `render.js`: 블록 6종 렌더링. `p`의 인라인 마크업(`**굵게**`, `` `코드` ``)은 **직접 파싱**(라이브러리 금지). 
   ⚠️ 모든 텍스트는 `textContent`로 삽입하고, 마크업 변환 시에도 XSS 방지를 위해 이스케이프 후 조립한다.
2. 코드 블록 복사 버튼, `predict` 블록 인터랙션.
3. 섹션 읽음 처리 → `cstudy:progress` 반영 → 홈 진도 막대 연동.
4. 단원 말미 "다음 단원 예고" 박스.

**DoD**: `ch01` 개념 탭에서 6종 블록이 모두 의도대로 보이고, 섹션을 읽으면 홈 진도 막대가 증가하며 새로고침 후에도 유지된다.

---

### Stage 4 — 퀴즈 엔진
1. `quiz.js`: 4종 문항 타입 채점.
   - `blank`는 공백 제거·소문자 변환 후 비교, `answer` 배열의 어느 하나와 일치하면 정답.
2. 문항 셔플(Fisher–Yates), 문항 수 선택.
3. 선택 즉시 채점 + 해설 펼침, 진행 표시.
4. 결과 화면, "오답만 다시 풀기".
5. 오답 → `cstudy:wrong` 기록, `quizBest` 갱신.

**DoD**: 5문항 세션을 끝까지 진행 → 결과 화면 정확 → 새로고침 후 오답노트에 남아 있다.

---

### Stage 5 — 오답노트 + 복습 큐
1. `#/wrong` 화면: 단원별 그룹, 재풀이 진입.
2. `review.js`: §4.4 간격 규칙. 홈 배너 연동.
3. 복습 세션 UI는 퀴즈 엔진 재사용(중복 구현 금지).

**DoD**: 오답 발생 → 다음 날 `due`가 되는 항목이 홈 배너에 집계된다(시스템 시각 변경으로 검증).

---

### Stage 6 — 기타 탭 + 용어사전
1. 기타 탭 4개 카드(§4.3-④).
2. `glossary.json` + `#/glossary` 검색(단순 부분 문자열 필터).
3. 설정: 테마 3택, 글자 크기 3단계.

**DoD**: `trap` 팁이 자동 수집되어 체크리스트로 표시된다.

---

### Stage 7 — PWA + 배포
1. `manifest.webmanifest`(이름, 아이콘 192/512, `display:standalone`, `theme_color`).
2. `sw.js`: 앱 셸은 캐시 우선, `content/*.json`은 **네트워크 우선 → 실패 시 캐시**(콘텐츠 갱신 반영을 위해). 캐시 버전 상수 `CACHE_VERSION`을 두고 변경 시 구캐시 삭제.
3. GitHub Pages 배포. 경로는 상대경로만 사용(`./js/app.js`) — 서브디렉터리 배포에서 절대경로는 깨진다.
4. README에 "홈 화면에 추가" 방법 기재.

**DoD**: 폰에서 URL 접속 → 홈 화면 추가 → **비행기 모드에서 실행 시 이미 열어본 단원이 정상 동작**한다.

---

### Stage 8 — 콘텐츠 채우기 (반복 작업)
- 코드는 더 이상 건드리지 않는다. `content/chXX.json` 추가/수정과 `index.json`의 `status` 변경만 수행.
- 단원 1개 기준 권장 분량: 섹션 4~7개, 블록 25~40개, 퀴즈 12~20문항, `trap` 팁 최소 3개.
- 새 단원 추가 시 반드시 스키마 검증 스크립트로 확인(§7).

---

## 7. 검증 & 품질 기준

- **스키마 검증**: `tools/validate.js`(Node 표준 모듈만 사용) — 필수 필드 누락, 미지원 블록/퀴즈 타입, `answer` 인덱스 범위 초과, 중복 `qid`를 검사하고 실패 시 종료 코드 1. Stage 4 이후 아무 때나 생성해도 되지만 Stage 8 이전에는 반드시 있어야 한다.
- **콘솔 에러 0개**가 각 Stage DoD의 암묵적 조건.
- 접근성 최소선: 시맨틱 태그, 버튼은 `<button>`, 포커스 표시 유지, 색만으로 정답/오답을 구분하지 않고 아이콘·텍스트 병기.
- `prefers-reduced-motion` 존중.

---

## 8. Claude Code 투입 지시문 (그대로 복사해 사용)

```
SPEC.md를 읽고 Stage 0부터 진행한다.
규칙:
1) 한 번에 한 Stage만 작업한다. 완료하면 DoD 체크 결과를 보고하고 멈춘다.
2) 프레임워크·번들러·npm 패키지를 도입하지 않는다. 바닐라 JS(ES Modules)만 쓴다.
3) §3 데이터 스키마를 바꾸지 않는다. 바꿔야 한다고 판단되면 코드를 수정하지 말고 먼저 이유를 보고한다.
4) 모바일 세로 화면(390×844) 기준으로 작업하고, 가로 스크롤이 생기면 그 자체를 버그로 취급한다.
5) 각 Stage 종료 시 변경 파일 목록과 확인 방법을 한 줄씩 정리해 보고한다.
```

---

## 9. 후속 확장 후보 (지금은 구현하지 않음)

- 학습 통계 화면(단원별 정답률, 태그별 약점).
- 진도 데이터 JSON export/import — 기기 교체·데스크톱 병행 대비.
- 코드 빈칸 채우기 문항 타입(`fillcode`) 추가.
- 단원 간 연계 문항(예: 포인터+함수 혼합 세트).
